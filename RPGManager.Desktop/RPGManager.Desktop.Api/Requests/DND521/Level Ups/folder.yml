info:
  name: Level Ups
  type: folder
  seq: 1

request:
  auth: inherit
  scripts:
    - type: after-response
      code: |-
        if (res.getStatus() === 200) {
          const reqName = req.getName().replace(" ", "_");
          const id = res.body;  
          const varName = `${reqName}`
          
          console.log(`${name}: ${id}`);
          bru.setVar(varName, id);
        }
