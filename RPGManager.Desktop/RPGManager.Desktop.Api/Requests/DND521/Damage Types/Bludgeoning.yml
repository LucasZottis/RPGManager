info:
  name: Bludgeoning
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/damage-type/create"
  body:
    type: json
    data: |-
      {
        "Name": "Contundente"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
