info:
  name: Piercing
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/damage-type/create"
  body:
    type: json
    data: |-
      {
        "Name": "Perfurante"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
