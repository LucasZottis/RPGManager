info:
  name: Simple
  type: http
  seq: 1

http:
  method: POST
  url: weapon-category/create
  body:
    type: json
    data: |-
      {
        "Name": "Simple"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
