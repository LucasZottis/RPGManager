info:
  name: Bell
  type: http
  seq: 9

http:
  method: POST
  url: "{{url}}/{{gear_partial_url}}/create"
  body:
    type: json
    data: |-
      {
        "GameSystemId": "{{GameSystem}}",
        "CostCurrencyTypeId": "{{Gold_Piece_Currency_Type}}",
        "Name": "Sino",
        "Description": "Quando tocado como uma ação de utilização, um sino produz um som que pode ser ouvido a até 60 pés de distância.",
        "Cost": 1,
        "Weight": 0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
