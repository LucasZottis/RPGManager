info:
  name: Medium
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/armor-category/create"
  body:
    type: json
    data: |-
      {
        "GameSystemId": "{{GameSystem}}",
        "Name": "Médio",
        "Description": "5 minutos para vestir e 1 minuto para despir"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
