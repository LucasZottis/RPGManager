info:
  name: Heavy
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/armor-category/create"
  body:
    type: json
    data: |-
      {
        "GameSystemId": "{{GameSystem}}",
        "Name": "Pesada",
        "Description": "10 minutos para vestir e 5 minutos para despir"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
