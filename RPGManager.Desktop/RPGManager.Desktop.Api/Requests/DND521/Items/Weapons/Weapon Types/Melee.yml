info:
  name: Melee
  type: http
  seq: 1

http:
  method: POST
  url: weapon-type/create
  body:
    type: json
    data: |-
      {
        "Name": "Corpo-a-Corpo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
