info:
  name: Sage
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/background/create"
  body:
    type: json
    data: |-
      {
        "gameSystemId": "{{GameSystemId}}",
        "name": "Sábio",
        "description": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
