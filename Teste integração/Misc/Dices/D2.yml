info:
  name: D2
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/dice-type/create"
  body:
    type: json
    data: |-
      {
        "name": "D2",
        "sides": 2
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
