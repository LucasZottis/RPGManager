info:
  name: D100
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/dice-type/create"
  body:
    type: json
    data: |-
      {
        "name": "D100",
        "sides": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
