info:
  name: Slashing
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/damage-type/create"
  body:
    type: json
    data: |-
      {
        "Name": "Cortante"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
