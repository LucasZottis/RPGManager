info:
  name: Nature
  type: http
  seq: 11

http:
  method: POST
  url: "{{url}}/skill/create"
  body:
    type: json
    data: |-
      {
        "BaseAbilityScoreId": "{{Intelligence}}",
        "Name": "Natureza",
        "Description": "Seu teste de Inteligência (Natureza) mede sua capacidade de recordar informações sobre terreno, plantas e animais, clima e ciclos naturais."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
