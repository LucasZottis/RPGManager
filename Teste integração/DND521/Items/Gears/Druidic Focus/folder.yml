info:
  name: Druidic Focus
  type: folder
  seq: 3

request:
  scripts:
    - type: after-response
      code: |-
        if (res.getStatus() === 200) {
          const name = req.getName().replace(" ", "_");
          const id = res.body;  
          const varName = `${name}_Gear`
          
          console.log(`${name}: ${id}`);
          bru.setVar(varName, id);
        }
