info:
  name: Parchment
  type: http
  seq: 63
http:
  method: POST
  url: "{{url}}/{{gear_partial_url}}/create"
  body:
    type: json
    data: |-
      {
        "GameSystemId": "{{GameSystem}}",
        "CostCurrencyTypeId": "{{Silver_Piece_Currency_Type}}",
        "Name": "Pergaminho",
        "Description": "Uma folha de Pergaminho pode conter aproximadamente 250 palavras escritas à mão.",
        "Cost": 1,
        "Weight": 0
      }
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
