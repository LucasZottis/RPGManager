info:
  name: Map
  type: http
  seq: 58
http:
  method: POST
  url: "{{url}}/{{gear_partial_url}}/create"
  body:
    type: json
    data: |-
      {
        "GameSystemId": "{{GameSystem}}",
        "CostCurrencyTypeId": "{{Gold_Piece_Currency_Type}}",
        "Name": "Mapa",
        "Description": "Se você consultar um Mapa preciso, você obtém um bônus de +5 nos testes de Sabedoria (Sobrevivência) que fizer para encontrar seu caminho no local representado nele.",
        "Cost": 1,
        "Weight": 0
      }
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
