info:
  name: Criminal
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/background/create"
  body:
    type: json
    data: |-
      {
        "gameSystemId": "{{GameSystemId}}",
        "name": "Criminoso",
        "description": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
