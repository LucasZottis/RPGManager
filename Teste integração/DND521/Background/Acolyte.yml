info:
  name: Acolyte
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/background/create"
  body:
    type: json
    data: |-
      {
        "gameSystemId": "{{GameSystemId}}",
        "name": "Acólito",
        "description": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
